<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.akitektuo.smartlist2">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".SmartList"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.Light">
        <activity android:name=".activity.MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name=".activity.LoginActivity"
            android:resizeableActivity="false"
            android:screenOrientation="portrait" />
        <activity android:name=".activity.CategoriesActivity" />
        <activity android:name=".activity.AdaptiveActivity" />
        <activity android:name=".activity.AutoFillActivity" />
        <activity android:name=".activity.RecommendationsActivity" />
        <activity android:name=".activity.CurrencyActivity" />
        <activity android:name=".activity.StatisticsActivity" />
        <activity android:name=".activity.ThemeActivity" />
        <activity android:name=".activity.AddCategoryActivity" />
        <activity android:name=".activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.EditCategoryActivity" />
        <activity android:name=".activity.CategoryActivity"></activity>
    </application>

</manifest>